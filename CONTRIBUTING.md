# 贡献指南

感谢你有兴趣参与，喵～ ᐢ..ᐢ

先说清楚**两件当前事实**：

1. **CI 已经在跑了**（[`.github/workflows/ci.yml`](.github/workflows/ci.yml)）。你提交 / 更新 PR 时 GitHub 会**自动**跑这些检查，结果直接显示在 PR 页面上：

   | 检查 | 现在会怎样 |
   |---|---|
   | 契约样例校验（合法 JSON + 身份字段必须是占位值） | ✅ 真在跑 |
   | Markdown 相对链接检查 | ✅ 真在跑 |
   | 边界守卫（内部资料目录不得入库） | ✅ 真在跑 |
   | 构建与测试 | ⏭️ **如实跳过** —— 仓库现在还没有代码；等出现第一个 `.csproj` 它会**自动开始生效** |

   → **请不要忽略红叉。** 绿了再找 maintainer review，大家都省事。

2. **本项目还在设计阶段，还没有代码** → 下面有些步骤是**为将来准备的**，现在跑不起来。

---

## 一、开始之前，请先读这三份

> 🚀 **如果是第一次参与**（包括"没什么基础"的同学）：**先看 [`docs/onboarding.md`](docs/onboarding.md)** ——
> 那里有克隆、环境、验证设置、第一个任务的完整步骤。下面三份是**准备读代码时**该看的。

| 顺序 | 文档 | 为什么 |
|---|---|---|
| 1️⃣ | [`docs/architecture.md`](docs/architecture.md) | **分层与三个接缝** —— 不读这个，改代码就是蒙的 |
| 2️⃣ | [`docs/standards/csharp-编码规范.md`](docs/standards/csharp-编码规范.md) | 编码规范，**提交前必读** |
| 3️⃣ | [`docs/contracts/README.md`](docs/contracts/README.md) | 契约样例怎么回事、为什么它是个安全网 |

> 💡 想快速上手，直接从 architecture.md **§8「我要改 XX，该动哪里？」** 那张表找你的场景。

---

## 二、环境要求

| 需要 | 版本 / 说明 |
|---|---|
| **.NET SDK** | **10.0**（本项目基线；`net10.0`） |
| 操作系统 | 开发不限；但 **`Client`（WPF）只能在 Windows 上构建**，其余项目必须能在 Linux 构建 |
| NapCat | **可选** —— 只有要联调 QQ 才需要装 |
| 编辑器 | 不限（VS / Rider / VS Code 都行） |

---

## 三、本地设置

```powershell
git clone https://github.com/xrzxrzx/ZipperAIQQ.git
cd ZipperAIQQ
```

### ⚠️ 协作者额外一步（**很重要，别跳过**）

本项目有**两份内容**：公开的代码仓库，以及一份**独立的私有仓库**（内部设计文档、决策记录）。

maintainer 会把你加进那个私有仓库。它的工作树放在代码仓库里的 `ai-docs/` 目录。**你需要把它排除掉**：

```powershell
# 在代码仓库根目录执行
Add-Content -Path '.git\info\exclude' -Value "ai-docs/"
```

- 用 **`.git/info/exclude`**（**本地、不进版本库**），**不要**用 `.gitignore`
  —— 后者会把这一行**公开出去**，等于告诉全世界这里有个私有夹
- **这一步是每个克隆都要各自做一次的**：`info/exclude` 是本地文件，不随仓库分发
- 忘了做的后果：`git add -A` 会试着把内部文档提交进**公开仓库**（那就泄漏了）

---

## 四、分支与提交

### 分支

- `main` 是**受保护**的主线，**不要直推**
- 改动走独立分支：`feat/xxx`、`fix/xxx`、`docs/xxx`、`chore/xxx`

### 提交信息：约定式提交

```
feat(agent): 支持工具调用超时配置
fix(napcat): 修复 echo 响应错配导致挂死
docs: 补充架构说明的术语表
chore: 升级依赖
test(transport): 补分帧边界用例
```

常用类型：`feat` / `fix` / `docs` / `test` / `refactor` / `chore` / `perf` / `build` / `ci`

- **一个提交做一件事** —— 混合提交会让 review 很痛苦
- 描述写**为什么**，而不只是**做了什么**

### 合并方式

- 一律 **Squash Merge**（保持 `main` 线性）

### ⛔ 禁止

- **禁止 `rebase` / `force-push` 改写已经推送的历史** —— 会破坏所有其他人的本地副本

---

## 五、提交 PR 之前

1. **自己在本地先跑一遍**（等有代码之后）：
   ```powershell
   dotnet build --configuration Release
   dotnet test
   ```
   > CI 也会跑，但**本地先跑一遍更省事** —— 不用等 CI 跑完才发现红叉。

2. **如果你改了契约**（`docs/contracts/*.json`）：确认**两端断言测试都更新**了，
   而且顺序是**先改样例**（见下面 §7）。
   > ⚠️ CI 会校验：样例必须是合法 JSON，且 **`selfUin` / `userId` / `groupId` 等身份字段只能是占位值**。

3. **自查有没有把不该提交的东西带进来**：
   ```powershell
   git status --short
   ```
   尤其确认 `ai-docs/` 没出现在列表里。
   > ⚠️ CI 里有一条守卫会拦这个 —— 但**被 CI 拦下就已经算提交过一次了**，自己先看更体面。

### PR 描述请包含

| 项 | 说明 |
|---|---|
| **改了什么** | 一两句说清 |
| **为什么** | 关联的 issue 或动机 |
| **怎么验的** | ⭐ **必须写** —— CI 只能验"机械可判定"的东西（JSON 合法、链接不断、能编译、测试过）；**你实际怎么试的、看到什么结果，只有你能说清** |
| **影响面** | 有没有碰契约 / 数据 / 权限；有没有破坏性改动 |

---

## 六、⚠️ 危险区（改动前请先开 issue 讨论）

这几处**改错的代价是安全漏洞或数据损坏**，不是"功能不好用"：

| 区域 | 为什么危险 |
|---|---|
| **权限门禁**（`GuardedAIFunction` / `PermissionGate`） | **接缝 C** —— 改错 = 群友能通过 LLM 执行危险操作 |
| **密钥与配置加载** | 改错 = 凭证泄漏或服务起不来 |
| **日志脱敏** | 改错 = **把密钥写进日志** |
| **WebSocket 传输层**（分帧 / 心跳 / 背压 / 并发发送 / 关闭握手 / 重连） | 六个坑，很多是**偶发**的，很难在本地复现 |
| **数据库 schema 变更** | 影响既有用户数据，需要迁移方案 |

**这些地方的 PR 必须由 maintainer review 后才能合并。**

---

## 七、契约改动（**本项目最容易出错的地方**）

客户端和服务端**各自维护一份同名 DTO**，所以改契约时**编译器不会提醒你对面**。

**正确顺序**：

1. **先改 [`docs/contracts/`](docs/contracts/) 里的 JSON 样例**
2. 服务端与客户端的断言测试**同时变红** → 强制你同步两端
3. 两边都绿 → 契约事实上一致

> 🔴 **硬纪律：样例里禁止出现真实标识**
> 所有 QQ 号 / 群号一律用占位号（如 `1000000000`），昵称用 `"示例昵称"`，
> 凭证字段一律 `null` 或 `""`。**详情见 [`docs/contracts/README.md`](docs/contracts/README.md)。**

---

## 八、关于 AI 与本项目

**本项目的实现代码由人类作者编写**，不采用 AI 直接生成实现代码的方式。

对**贡献者**同样适用：

| ✅ 可以 | ❌ 不可以 |
|---|---|
| 用 AI 解释原理、讲清为什么这样设计 | **直接产出你要提交的实现代码** |
| 用 AI 帮你排查思路、找线索 | 让 AI 替你写补丁 |
| 用 AI 检查你的代码（**你是作者，AI 是审查者**） | 生成可复制粘贴的完整答案 |

**理由很直接**：代写会让你**失去学习机会**；而且外人**无法分辨**这段代码是人写的还是 AI 写的 ——
那等于**伪造贡献记录**。

> **「指出问题」和「写出代码」之间有一条线**，这条线不许模糊。

---

## 九、想找点活干？

**适合新手的**（门槛低、改动独立、改坏了也不会伤到别人）：

| 任务 | 说明 |
|---|---|
| 🟢 **写一个工具（`AIFunction`）** | 最推荐 —— 新增一个类 + 一行注册，**完全不碰架构** |
| 🟢 **补契约样例 / 补测试用例** | 照已有的抄结构改断言即可 |
| 🟢 **改文档、加截图** | 门槛最低，但对项目价值真实 |
| 🟡 **改桌面界面（WPF/XAML）** | 表现层独立，改错不会炸服务端 |

**不太适合新手的**：

- 🔴 加新的 `IQQChannel` 实现（涉及协议与连接生命周期）
- 🔴 碰 §6 危险区里的任何东西

> 拿不准某个任务适不适合你 —— **直接开 issue 问**，说明你想做什么、你的经验大概在哪一档。这不会显得你菜。

---

## 十、安全与隐私（⚠️ 请遵守）

1. **不要把密钥 / token 提交进仓库** —— 一律走环境变量或 `appsettings.Local.json`（已在 `.gitignore`）
2. **不要在 issue / PR 里贴二维码截图** —— **二维码等同于账号接管凭证**
3. **贴日志前先脱敏** —— 里面可能有 QQ 号、群号、token
4. **不要提交真实 QQ 号 / 群号** —— 包括契约样例、测试数据、截图
5. **QQ 账号风险自担**：NapCat 是非官方协议端，**使用它可能导致账号被限制**

发现**安全漏洞**？请**不要**公开开 issue —— 先私下联系 maintainer。

---

## 十一、行为与沟通

- **对事不对人**：review 意见是针对代码，不是针对你
- **问题问得具体一点**：你做了什么 → 期望什么 → 实际看到什么 → 报错原文。
  这样别人才能帮上忙（也才愿意帮）
- **新手提问完全欢迎** —— 这个项目本身就是带着"有没有基础都能参与一点"的想法开的

---

## 十二、许可证

本项目使用 [MIT 许可证](LICENSE)。你提交的贡献将同样以 MIT 许可发布。

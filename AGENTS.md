# ZipperAIQQ 项目规则

> 本文件是本项目的最高优先级约定，**优先于任何通用工作规范**。
> 冲突时以本文件为准。

---

## 一、⚠️ 铁律：代码由用户本人编写

**本项目的所有实现代码由用户本人编写。**

AI 助手（任何模型、任何工具、任何会话）在本项目中的职责边界：

| | 职责 |
|---|---|
| ✅ **可以做** | 写设计文档、架构方案、接口契约、数据模型设计 |
| ✅ **可以做** | 铺基础设施：仓库、目录结构、编码规范、文档骨架、`.gitignore` |
| ✅ **可以做** | Review 用户写的代码、指出问题、解释原理、排查 bug、答疑 |
| ✅ **可以做** | 运行验证命令、跑测试、看日志、做只读排查 |
| ❌ **不可以做** | **编写、修改实现代码**（除非用户在当前会话中**明确要求**） |
| ❌ **不可以做** | 「顺手帮你改一下」「我直接实现了」——**这类主动代劳一律禁止** |

**这条规则的理由**：用户要自己掌握实现（2026-09-21 明确声明）。

**唯一例外**：用户在**当前会话**中明确说「你帮我写 / 你来实现 / 直接改」。此时也仅限于**明确说出的范围**，不得扩散。

> 违反这条的后果最严重：它会让用户失去对代码的掌控，也让 Review 失去意义（自己审自己）。

### 一.1 ⚠️ 同一个理由适用于**外部贡献者**（2026-09-22 用户批准）

**本项目计划开源，并有一位新手同学参与。对同学的 PR，AI 同样不许代写实现代码。**

| | 对同学的 PR，AI 可以 | 对同学的 PR，AI 不可以 |
|---|---|---|
| ✅ | 解释原理、讲清为什么这样设计 | ❌ **直接产出他要提交的实现代码** |
| ✅ | 指出问题、给出排查方向与思路 | ❌ 替他写补丁、改他的代码 |
| ✅ | Review 他的代码（他是作者，AI 是审查者） | ❌ 把「他写不出来」变成「AI 写出来」 |
| ✅ | 推荐该读哪份文档、哪段已有代码 | ❌ 生成可复制粘贴的完整答案 |

**理由与铁律本身完全相同**：代写会让贡献者**失去学习机会**；而且开源项目里**外人无法分辨**这段代码是人写的还是 AI 写的 —— 那等于**伪造贡献记录**。

> **边界感**：**「指出问题」和「写出代码」之间有一条线**，这条线不许模糊。
> 同学遇到不会的地方，正确做法是**引导他学会**，不是替他完成。

---

## 二、技术基线（已定，变更须记录）

| 项 | 结论 |
|---|---|
| 语言 / 运行时 | **C# / .NET 10**（`net10.0`，LTS）。用户既定：**新项目一律 .NET 10** |
| 服务端 | 跨平台（Windows + Linux），`Microsoft.Extensions.Hosting` + ASP.NET Core (Kestrel) |
| 客户端 | **WPF**（Windows-only，仅表现层；服务端必须保持跨平台） |
| **MVVM 框架** | **`CommunityToolkit.Mvvm`**（微软官方，Source Generator 驱动） |
| **IoC 容器** | **`Microsoft.Extensions.DependencyInjection`**（服务端由 `Microsoft.Extensions.Hosting` 提供，客户端手工建 `ServiceCollection`） |
| **公共基础设施** | **`ZipperAIQQ.Core`**（第 7 个项目）—— 配置骨架 / 日志装配。**准入规则：放进去之前先说出第二个消费者是谁**（否则它会变成垃圾桶），详见 `ai-docs/design/configuration-design.md` |
| **配置** | `Microsoft.Extensions.Configuration` + `IOptions<T>`；**Options 类型跟着消费者模块走**（不集中在 Core）；机密只走环境变量 / `appsettings.Local.json` |
| **日志** | **Serilog** —— 容器里注册 `Serilog.ILogger` 直接注入（`ForContext<T>()` 取上下文）；**写盘前脱敏** |
| 客户端通信 | **HTTP 单端点（非 REST）+ WebSocket**；**不引 gRPC** |
| QQ 通道 | **NapCat**（OneBot 11），抽象为 `IQQChannel`（正向 WS + 反向 WS 双实现） |
| AI | **MEAI（`Microsoft.Extensions.AI`）打底 + 部分自研** —— 自动工具循环用 MEAI 的 `FunctionInvokingChatClient`，OpenAI 兼容端点（DeepSeek）用 `Microsoft.Extensions.AI.OpenAI`；**自研**会话隔离 / 上下文压缩策略 / 工具注册与权限门禁 / `AgentEvent` 事件流。**不引 MAF**（理由见 `ai-docs/memory/decisions.md`） |
| **JSON 库** | **`System.Text.Json`（全项目统一）** —— 契约 DTO、OneBot 事件、LLM 报文、配置一律用它（**与 MEAI 同栈，避免双轨**） |
| 存储 | SQLite |

### 通信契约（详见 `ai-docs/architecture-proposal.md` §3.5）

- **HTTP 只发命令**：`POST /api`，body `{action, payload}`，响应 `{code, message, data}`
- **WebSocket 只收事件**：`WS /events`，帧 `{type, seq, time, data}`
- **鉴权**：loopback 免验；**非 loopback 强制 token，且启动时校验未配即拒绝启动**

---

## 三、架构约束（改代码前先读）

完整方案见 `ai-docs/architecture-proposal.md`（架构方案，**内部资料，不随本仓库公开**）。三个**不可破坏**的接缝：

| 接缝 | 约束 |
|---|---|
| **A. 表现层 ↔ 编排层** | 必须走 HTTP/WS，**不得进程内直连**——这是「本机单体」与「服务器分离」共用一套代码的前提 |
| **B. QQ 通道 ↔ 编排层** | 必须经 `IQQChannel` 抽象，**上层不得直接依赖 NapCat 类型** |
| **C. 工具执行 → 权限门禁** | 危险操作（执行命令、写/删文件、访问外部服务）**默认仅管理员**；门禁放在**服务端** |

---

## 四、开发流程

本项目遵循**四阶段门控**：`S1 需求 → S2 技术方案 → S3 开发 → S4 测试`。

- **S1、S2 必须经用户批准**才能进入下一阶段
- S3 阶段**由用户本人编码**（见 §一）；助手的产出是**设计文档**
- 设计文档必须包含：**改动点清单（= 已声明文件）、接口定义、验收标准、测试策略**
- **不得跳过文档直接实现**

### 遇到模糊需求时
给出 2–3 个选项 + 各自代价与收益，**由用户拍板**，不替用户决定。

---

## 五、文档与记忆

### 两个面（2026-09-22 定）

| 面 | 位置 | 会不会公开 |
|---|---|---|
| 🟢 **公开面** | `docs/contracts/`、`docs/standards/`、`README.md`、`LICENSE`、`AGENTS.md`、`.github/`（待建） | **随代码开源** |
| 🔒 **私有面** | **`ai-docs/`** —— 独立 git 仓库（永久私有），被外层仓库通过 `.git/info/exclude` 忽略 | **永不公开**，与协作者共享 |

**私有面内部**（`ai-docs/`）：

| 位置 | 用途 |
|---|---|
| `ai-docs/architecture-proposal.md` | 架构方案（单一事实来源） |
| `ai-docs/design/` | 详细设计（`technical-design-v1` / `transport-design` / `agent-design` / **`configuration-design`** / `open-source-readiness`） |
| `ai-docs/research/` | 调研报告（如 MAF 可行性） |
| `ai-docs/memory/` | **项目本地记忆**：决策、进度、踩坑、教训 |

- **决策一旦改变**，必须同步更新架构文档 + 记忆，**不得只在对话里说**
- 记忆写入规则：项目内的事写 `ai-docs/memory/`；跨项目通用的结论另写全局记忆库

> 完整边界规则与落地步骤见 `ai-docs/design/open-source-readiness.md` §2（**内部资料，不随本仓库公开**）。

### ⚠️ 跨边界的两条禁令

1. **公开面里不许出现指向 `ai-docs/` 的 Markdown 链接** —— 公开仓库里没有这个目录，链接**必然变死链**。
   （**纯文字路径可以写**，但要标明"内部资料，不随仓库公开" —— 比如本文件为了说清规矩就必须点名它）
2. **公开面里不许写内部决策过程、服务器信息、踩坑记录** —— 只讲"是什么、怎么用、别破坏哪三个接缝"

> 📌 **本文件（`AGENTS.md`）本身在公开面**（2026-09-22 定）—— 它声明"AI 不代写实现代码"是这个项目的公开立场。
> 因此它**不能**包含上面第 2 条禁止的内容。

---

## 六、Git 约定

- 提交信息用**约定式提交**格式（`feat:` / `fix:` / `docs:` / `chore:` 等）
- **禁止** `rebase` / `force-push` 改写已推送历史；合并一律 Squash Merge
- **阶段成果收尾即提交**，不等"验收通过"
- **只提交、不推送**：`git push` 必须由用户明确要求

---

## 七、安全约定

- **机密绝不进仓库**：token、密码、API key 一律走环境变量或 `appsettings.Local.json`（已在 `.gitignore` 中）
- **QQ 密码不落盘**：优先扫码登录
- 服务端监听非 loopback 时**必须**配 token（见 §二）
